(function () {
  'use strict';

  // Add in margin for activity panel headings. If no JS then presentation
  // is slightly peculiar
  var css = document.createElement("style");
  css.type = "text/css";
  css.innerHTML = "#main .inner .f_panelHead h2 {margin-left: 1.5em;} #main .inner .f_panelHead > p {margin-left: 2em;}";
  document.getElementsByTagName("head")[0].appendChild(css);
}());